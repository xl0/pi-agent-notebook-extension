# VSCode as the notebook UI; no custom Pi TUI

The extension does not ship a notebook-specific TUI or UI layer. VSCode is the expected notebook frontend — it already provides a high-quality notebook editing experience that would be costly and low-value to duplicate inside Pi.
